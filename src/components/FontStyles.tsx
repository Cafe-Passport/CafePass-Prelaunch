'use client';

export default function FontStyles() {
  return (
    <style jsx global>{`
      :root {
        --font-special-gothic: 'Special Gothic Expanded One', system-ui, -apple-system, sans-serif;
      }
    `}</style>
  );
}
